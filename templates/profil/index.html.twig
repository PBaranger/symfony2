{% extends 'home.html.twig' %}
{% block title %}Mon profil !
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.commentaire {
			text-align: center;
			margin: 10px 0;
			background-color: #e9ecef;
			padding: 10px;
		}
	</style>

	<div class="dcard">
		<div class="large-wrapper">

			<table class="table table-hover table-bordered">
				<tr>
					<td>Id</td>
					<td>Email</td>
					<td>Role</td>
				</tr>

				<tr>
					<td>{{user.id}}</td>
					<td>{{user.email}}</td>
					<td>{{ user.roles|join(', ') }}</td>
				</tr>


			</table>
		</div>
		<a href="{{ path('app_profil_edit', {id: user.id}) }}" class="btn btn-warning">Éditer</a>
		<a href="{{ path('delete_article', {id: user.id}) }}" class="btn btn-danger">Supprimer</a>

	</div>
{% endblock %}
